version: '3.3'

services:
 backend:
  container_name: PYTHON-API-BACKEND
  build: .
  command: python3 main.py
  volumes:
    - .:/opt/imf
  links:
    - db
 db:
  image: mongo:latest
  container_name: MONGO-API-BACKEND
  ports:
    - "27017:27017"
  volumes:
    - .:/opt/imf/data
